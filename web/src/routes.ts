// eslint-disable-next-line @typescript-eslint/no-var-requires -- using inline loaders doesn't allow us to use imports w/ ts
const routes = require("react-router-routes-loader!./pages");

export default routes;
